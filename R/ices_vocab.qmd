```{r init}
#| echo: FALSE
#| warning: FALSE
#| message: FALSE
#| results: 'hide'
getUsername <- function(){
	name <- Sys.info()[["user"]]
	return(name)
}

#if (!grepl("montepomi", getwd())) {
if(getUsername() == 'joliviero'){
setwd("D:/workspace/DIASPARA_WP3_migdb/R")
} else if (getUsername() == 'cedric.briand'){
setwd("C:/workspace/DIASPARA_WP3_migdb/R")
}
library(RPostgres)
library(sf)
library(getPass)
library(readxl)
library(flextable)
library(tidyverse)
library(yaml)
library(DBI)
library(knitr)
library(kableExtra)
library(icesVocab)
cred=read_yaml("../credentials.yml")
con_migdb = dbConnect(Postgres(), dbname=cred$dbnamesalmo,host=cred$host,port=cred$port,user=cred$userdiaspara, password=cred$passworddiaspara)
```

``` {r icesVocab_test}
code_type <- getCodeTypeList()
knitr::kable(code_type, caption = "Number of locations per var_mod") %>% kable_styling(bootstrap_options = c("striped", "hover", "condensed"))

#code_list <- 
```